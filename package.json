{
  "name": "live-translator",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "db:push": "drizzle-kit push",
    "test": "turbo test",
    "test:watch": "turbo test:watch",
    "test:e2e": "pnpm -C apps/web test:e2e",
    "lint": "pnpm -C apps/server exec tsc --noEmit && pnpm -C apps/web exec tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^25.0.1",
    "drizzle-kit": "^0.31.8",
    "tsx": "^4.21.0",
    "turbo": "^2.2.0",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.45.1",
    "postgres": "^3.4.4"
  },
  "type": "module"
}
